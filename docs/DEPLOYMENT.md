# 🚀 Deployment & Setup Guide

## Предварительные требования
*   Node.js 18.17 или выше.
*   Аккаунт Google Cloud / AI Studio для получения API ключа.

## Локальный запуск

1.  **Клонирование репозитория**:
    ```bash
    git clone <repo-url>
    cd ai-ad-generator
    ```

2.  **Установка зависимостей**:
    ```bash
    npm install
    ```

3.  **Настройка окружения**:
    Создайте файл `.env` (или `.env.local`) в корне проекта:
    ```env
    # Обязательно: Ключ от Google AI Studio
    # Получить здесь: https://aistudio.google.com/app/apikey
    NEXT_PUBLIC_API_KEY=AIzaSy...

    # Опционально: Google Analytics 4 Measurement ID
    NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
    ```

4.  **Запуск сервера разработки**:
    ```bash
    npm run dev
    ```
    Приложение будет доступно по адресу `http://localhost:3000`.

## Сборка для продакшна

1.  **Билд**:
    ```bash
    npm run build
    ```
    Next.js создаст оптимизированную сборку в папке `.next`.

2.  **Запуск**:
    ```bash
    npm start
    ```

## Деплой на Vercel (Рекомендуется)

Этот проект оптимизирован для деплоя на Vercel (создателей Next.js).

1.  Запушьте код в GitHub/GitLab.
2.  Зайдите на [vercel.com](https://vercel.com) и нажмите "Add New Project".
3.  Импортируйте репозиторий.
4.  В разделе **Environment Variables** добавьте:
    *   `NEXT_PUBLIC_API_KEY`
    *   `NEXT_PUBLIC_GA_MEASUREMENT_ID`
5.  Нажмите **Deploy**.

## Чек-лист перед релизом

*   [ ] Проверить, что в `metadata` (layout.tsx) указан корректный URL сайта для OpenGraph.
*   [ ] Обновить `sitemap.ts` и `robots.ts` с реальным доменом.
*   [ ] Убедиться, что API Key имеет настроенные квоты (billing) в Google Cloud Console, если планируется высокая нагрузка.
*   [ ] Проверить работу мобильной версии на реальном устройстве.