# 🚀 AI Генератор Объявлений (AI Classifieds Ad Generator)

Современное SaaS-приложение, помогающее продавцам создавать высококонверсионные описания товаров для классифайдов (Avito, Юла, OLX) с использованием искусственного интеллекта Google Gemini.

![App Screenshot](https://via.placeholder.com/800x400?text=AI+Ad+Generator+Preview)

## 📋 О проекте

Приложение решает проблему "чистого листа" для продавцов. Вместо того чтобы мучительно придумывать описание для старого iPhone или автомобиля, пользователь вводит базовые характеристики, загружает фото, и AI генерирует продающий текст, адаптированный под выбранный тон и категорию.

## 🔄 Флоу работы приложения (User Flow)

Процесс создания объявления построен максимально линейно для удобства пользователя:

1.  **Выбор категории**: Пользователь начинает с выбора одной из 4 категорий (Электроника, Авто, Услуги, Одежда). Интерфейс мгновенно перестраивается, подтягивая уникальный набор полей (например, "Пробег" для авто или "Размер" для одежды).
2.  **Ввод данных**: Заполняются характеристики. Валидация в реальном времени подсвечивает обязательные поля.
3.  **Загрузка фото (Мультимодальность)**: Пользователь может загрузить изображение товара.
    *   *Под капотом*: Изображение конвертируется в Base64.
    *   *AI*: Gemini анализирует картинку и добавляет в текст визуальные детали (например, "на корпусе есть небольшая царапина" или "цвет глубокий синий"), что делает объявление честным и детальным.
4.  **Выбор тона (Tone of Voice)**: Пользователь задает настроение текста:
    *   *Агрессивный* — для быстрой продажи.
    *   *Вежливый* — для детских вещей или услуг.
    *   *Человечный* — имитация обычного продавца с легким сленгом.
5.  **Генерация**:
    *   При клике проверяется локальный лимит запросов (Rate Limiting).
    *   Формируется сложный промпт.
    *   Отправляется запрос к API `gemini-3-flash-preview`.
6.  **SEO Оптимизация (Post-processing)**:
    *   После генерации доступна кнопка "Прокачать SEO".
    *   Приложение делает второй запрос к AI, прося выступить в роли SEO-специалиста, подобрать высокочастотные ключи и нативно вписать их в текст.
7.  **Финал**: Пользователь копирует текст или шерит его через нативное меню телефона.

## 🛠 Технологический стек

### Frontend
- **React 19**: Использование новейших хуков и возможностей рендеринга.
- **TypeScript**: Строгая типизация для надежности кода.
- **Tailwind CSS**: Утилитарный CSS для быстрого и адаптивного UI/UX.
- **Lucide React**: Легковесные и красивые иконки.
- **Vite**: Сверхбыстрая сборка и Hot Module Replacement (HMR).

### Artificial Intelligence
- **Google Gemini API (gemini-3-flash-preview)**:
  - **Multimodal**: Анализирует загруженные изображения.
  - **JSON Mode**: Используется для структурированного возврата данных при SEO-оптимизации.

### State & Persistence
- **LocalStorage**: Сохранение состояния формы (черновики), темы и счетчиков квот.

---

## 📊 Аналитика (Google Analytics 4)

В приложение интегрирована система аналитики для понимания поведения пользователей и оптимизации продукта.

### Реализация
В файле `services/analytics.ts` находится легковесная обертка над `gtag.js`. Скрипт загружается асинхронно только при наличии `GA_MEASUREMENT_ID`, что предотвращает ошибки в dev-режиме без ключей.

### Отслеживаемые события (Custom Events)
Мы не ограничиваемся стандартными просмотрами страниц. Настроены кастомные события для каждого ключевого действия:

| Событие | Описание | Параметры |
| :--- | :--- | :--- |
| `generate_ad_click` | Попытка генерации | `{ category: 'auto', tone: 'polite' }` |
| `generate_ad_success` | Успешный ответ AI | `{ category: 'auto' }` |
| `generate_ad_error` | Ошибка API (квоты, сеть) | `{ error: '429...' }` |
| `optimize_ad_click` | Клик по SEO-оптимизации | `{ category: '...' }` |
| `copy_ad_click` | Копирование текста | `{ category: '...' }` |
| `share_ad_click` | Использование кнопки "Поделиться" | `{ method: 'native' | 'modal' }` |
| `change_category` | Переключение вкладок | `{ category: 'electronics' }` |
| `toggle_theme` | Переключение темы | `{ theme: 'dark' }` |

### Как просматривать аналитику
1.  Зайдите на [analytics.google.com](https://analytics.google.com).
2.  Создайте новый "Поток данных" (Web Stream) и скопируйте **Measurement ID** (формат `G-XXXXXXXXXX`).
3.  Вставьте ID в `.env` файл вашего проекта.
4.  В интерфейсе GA перейдите в:
    *   **Отчеты (Reports) → В реальном времени (Realtime)**: Чтобы видеть действия пользователей прямо сейчас (удобно для тестов).
    *   **Вовлечение (Engagement) → События (Events)**: Чтобы смотреть агрегированную статистику по популярности категорий и конверсии в копирование.

---

## ✨ Ключевые возможности

1.  **Мульти-категорийность**: Поддержка специфических полей для Электроники, Авто, Услуг и Одежды.
2.  **Умные квоты**: Визуальный индикатор лимитов API (RPM) предотвращает блокировку ключа, показывая таймер до сброса.
3.  **Темная тема**: Полная поддержка Dark Mode.

---

## 🚀 Запуск проекта

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-username/ai-ad-generator.git
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройка окружения:**
   Создайте файл `.env` в корне проекта и добавьте ваши ключи:
   ```env
   API_KEY=ваш_ключ_от_google_ai_studio
   GA_MEASUREMENT_ID=G-XXXXXXXXXX
   ```

4. **Запуск:**
   ```bash
   npm run dev
   ```

---

## 📈 Масштабируемость и Архитектура

Текущая версия работает как **Client-Side Application** (запросы к AI идут прямо из браузера). Для масштабирования в полноценный SaaS необходимо внедрить Backend.

### 1. Переход на Backend (Next.js / Node.js / Python)
*   **Зачем**: Чтобы скрыть API Key и управлять сложной логикой.
*   **Реализация**: Проксирование запросов к Gemini через свой сервер.

### 2. База Данных (Рекомендации)
Для хранения пользователей, истории генераций и подписок:
*   **Supabase (PostgreSQL)**: Идеальный выбор. Дает Auth, Realtime DB и Edge Functions из коробки.
*   **Redis**: Для кэширования частых запросов и реализации Rate Limiting (ограничения частоты запросов) на уровне IP пользователя, а не глобально.

### 3. Интеграции
*   **Авто-постинг**: Интеграция с API Авито/Telegram для публикации объявлений одной кнопкой.
*   **Аналитика цен**: Парсинг площадок для подсказки "Рекомендуемая цена" на основе введенных данных.

---

## 💰 Монетизация: Система Кредитов (Pay-As-You-Go)

Вместо ежемесячной подписки, которая невыгодна редким продавцам, используется **Токеномика (Credit-based system)**. Пользователь покупает внутреннюю валюту (кредиты) и тратит её на генерации. Это гарантирует доход с каждого целевого действия (unit economics всегда положительная).

### 1. Экономика списаний (Cost Model)
Каждая функция имеет свою цену в зависимости от затрат ресурсов API и ценности для бизнеса пользователя:
*   **1 Кредит** = Генерация текста (Text-to-Text). Базовая услуга.
*   **2 Кредита** = Генерация по фото (Vision + Text). Более дорогая операция для сервера, дающая уникальный контент.
*   **1 Кредит** = SEO-Оптимизация. Второй вызов API для переработки текста.
*   **0.5 Кредита** = Рерайт (кнопка "Перегенерировать"). Скидка за повторную попытку, если первый вариант не устроил.

### 2. Пакеты пополнения (In-App Purchases)
Продажа токенов пакетами с прогрессивной скидкой (Upsell) для увеличения среднего чека:
*   **Starter (10 кредитов) — 99₽**.
    *   Для "одноразовых" продавцов личных вещей.
    *   *Цена генерации*: ~10₽. Высокая маржинальность (>90%).
*   **Seller (50 кредитов) — 390₽**.
    *   Для активного расхламления или мелких перекупов.
    *   *Цена генерации*: ~7.8₽.
*   **Pro Business (300 кредитов) — 1490₽**.
    *   Для профессиональных авитологов и магазинов.
    *   *Цена генерации*: ~5₽.
    *   *Бонус*: Доступ к массовой загрузке через Excel (Bulk Generation).

### 3. Механики удержания (Retention)
*   **Welcome Bonus**: 3 бесплатных кредита при регистрации. Достаточно, чтобы "подсесть" на качество (Aha-moment), но недостаточно для полноценной работы.
*   **Daily Reward**: 1 бесплатный кредит за ежедневный вход в приложение (стимулирует DAU и привычку).
*   **Referral**: "Приведи друга — получи 10 кредитов". Виральный рост.

### 4. B2B API
*   Продажа пакетов API-вызовов (от 10 000 кредитов) для интеграции в CRM ломбардов и комиссионных магазинов, желающих автоматизировать постинг.

---

## 🛡 Безопасность

*   В текущей версии API Key находится в переменных окружения сборки. **Не выкладывайте этот билд в открытый доступ на Vercel/Netlify без серверной прослойки**, иначе ваш ключ могут украсть.
*   Рекомендуется добавить Google Recaptcha для защиты от ботов.

---

## 🤝 Вклад в проект (Contributing)

1.  Fork it
2.  Create your feature branch (`git checkout -b feature/AmazingFeature`)
3.  Commit your changes (`git commit -m 'Add some AmazingFeature'`)
4.  Push to the branch (`git push origin feature/AmazingFeature`)
5.  Open a Pull Request
