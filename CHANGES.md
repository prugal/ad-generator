## 2026-03-19

### Fixed
- Исправлена ошибка `invalid-signature` при оплате через Robokassa за счет корректной сортировки `shp_` параметров.
- Устранена потеря сессии пользователя после возврата из платежной системы путем настройки `samesite=none` и `secure=true` для cookie Supabase.
